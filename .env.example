# A long random string used to encrypt session cookies. Generate one with:
#   openssl rand -base64 32
NUXT_SESSION_PASSWORD=

# Directory where the SQLite database and uploaded question images are stored.
# Must be a persistent path outside the app's build/deploy directory.
STUDY_BUDDY_DATA_DIR=./.data
